getset.controller("DashboardCtrl", function($scope) {
	
    $scope.model = {
        message: "dashboard"
    };

    /* var converter = new Markdown.Converter(),
     markdownToHtml = converter.makeHtml;
*/
    /*$.get("readme.md").success(function (data) {
        console.log(markdownToHtml(data));
    });*/
});
